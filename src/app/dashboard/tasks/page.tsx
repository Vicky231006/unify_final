"use client";
import { EmployeeView } from "@/components/dashboard/EmployeeView";

// /dashboard/tasks — Employee task list (re-uses the Teams-style card grid)
export default function TasksPage() {
    return <EmployeeView />;
}
